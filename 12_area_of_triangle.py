#Python program that will accept the base and height of a triangle and compute the area
t_base = int(input("Enter the base length of triangle: "))
t_height = int(input("Enter the height length of triangle: "))
area = (1/2) * t_base * t_height
print("Area of the triangle is ", area)